# Security and privacy

This project is a static, browser-only demo. It has no authentication, database, analytics, or server-side write endpoints.

The workout files in this repository are intentionally public. Credentials, environment files, and Vercel project-link metadata must remain in ignored local files and must never be committed.

If you find a security issue, use GitHub's private vulnerability reporting feature rather than opening a public issue.
